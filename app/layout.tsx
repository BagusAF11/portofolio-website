import type { Metadata } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
})

const playfair = Playfair_Display({
  subsets: ['latin'],
  variable: '--font-playfair',
})

export const metadata: Metadata = {
  title: 'Bagus Darmawan — Portfolio',
  description: 'Management Student & Creative Digital Enthusiast. Detail-oriented individual with experience in customer service, printing operations, Photoshop, communication, and digital work.',
  keywords: ['Bagus Darmawan', 'Portfolio', 'Management Student', 'Creative Digital', 'Design'],
}

export const viewport = {
  themeColor: '#111116',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable} bg-background`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
